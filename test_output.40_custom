#!/bin/sh
exec tail -n +3 $0
# This file provides an easy way to add custom menu entries.  Simply type the
# menu entries you want to add after this comment.  Be careful not to change
# the 'exec tail' line above.

menuentry "Clonezilla_Backup_Custom" {
    ISO="/clonezilla_custom.iso"
    search --set -f $ISO
    loopback loop $ISO

    linux (loop)/live/vmlinuz \
        boot=live union=overlay username=user config components quiet loglevel=3 \
        noswap edd=on nomodeset \
        locales=en_US.UTF-8 keyboard-layouts=us \
        ocs_prerun="ocs-scan-disk" \
        ocs_repository="dev:///UUID=11112222-3333-4444-5555-666677778888" \
        ocs_preload="mount --bind -o noatime /tmp/ocsroot_bind_root/my_backup_folder /home/partimag" \
        ocs_live_run="ocs-sr -batch -q2 -j2 -z9p -p reboot -sfsck -scs -senc savedisk /my_backup_folder/test_backup_name_here sda1" \
        ocs_live_batch="yes" \
        vga=791 i915.blacklist=yes radeonhd.blacklist=yes nouveau.blacklist=yes vmwgfx.enable_fbdev=1 scsi_mod.use_blk_mq=0 nvme.poll_queues=1 \
        findiso=$ISO

    initrd (loop)/live/initrd.img
}